#include <iostream>
#include <string>

int main() {
	/*
	if (5>3)
		std::cout << "얍\n";


	int a;
	std::cin >> a;

	if (a < 10)
		std::cout << "입력한 수가 10보다 작습니다";

	else if (a==10)
		std::cout << "10을 입력하셨군요";

	else
		std::cout << "입력한 수가 10봐다 큽니다";






	//성적

	int score;
	std::cout << "성적을 입력해주세요";
	std::cin >> score;
	if (score>100||score<0)
	{
		std::cout << "입려값이 잘못되었습니다";
	}
	else
	{
		
		if (score >= 100 && score <= 100)
			std::cout << "성적은 " << "A";
		
		else if (score >= 80) 
			std::cout << "성적은 " << "B";
		
		else if (score >= 70) 
			std::cout << "성적은 " << "C";
		
		else if (score >= 60) 
			std::cout << "성적은 " << "D";
		
		else 
			std::cout << "성적은 " << "F";
		
	}


	//중첩 if문

	std::string userId, userPw;
	std::string inputId = "user01", inputPw = "1234";

	std::cout << "아이디를 입력하세요 ";
	std::cin >> userId;
	std::cout << "비밀번호를 입력하세요 ";
	std::cin >> userPw;


	if (userId == inputId){
		if (userPw==inputPw)
			std::cout << "로그인 성공!!\n";
		
		else
			std::cout << "비밀번호가 틀렸습니다\n로그인 실패";
	}


	else
		std::cout << "아이디가 틀렸습니다\n로그인 실패";
	


	int b;
	std::cout << "숫자를 입력하세요";
	std::cin >> b;
	

	switch (b)  //case에 문자열은 올 수 없다 (문자는 가능)
	{

	case 3:
		std::cout << "3입니다";
		break;
	case 5:
		std::cout << "5입니다";
		break;

	default:
		std::cout << "예상한 수가 아닙니다";  //default는 따로 break를 걸어주지 않아도 된다 

	}
*/




}